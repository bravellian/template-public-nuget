# Incursa .NET Library Template

Template repository for private Incursa .NET libraries.

Primary layout:
- `src/` contains packable libraries.
- `tests/` contains unit/integration test projects.
- Repo-level build conventions are enforced through `Directory.Build.props` and related `*.props` files.
- Central package management is defined in `Directory.Packages.props`.

Primary solution:
- Default template solution is `Incursa.Template.slnx`.
- Consumers typically rename it when initializing from the template.

Core commands:
- Restore: `dotnet restore Incursa.Template.slnx`
- Build: `dotnet build Incursa.Template.slnx -c Release`
- Test: `dotnet test Incursa.Template.slnx -c Release`
- Pack: `dotnet pack Incursa.Template.slnx -c Release -o ./nupkgs`
